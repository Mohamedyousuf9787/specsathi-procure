export type ProductListingViewState = {
  status: "idle" | "loading" | "live" | "fallback";
  message?: string;
  listings: Array<{ id: string; title: string; merchant: string | null; priceText: string | null; rating: number | null; reviews: number | null; imageUrl: string | null; productUrl: string | null; delivery: string | null; availability: string | null; completeness: "complete" | "unverified"; policy: "eligible" | "approval_needed" | "blocked" | "unverified" }>;
};

export default function ProductListingsPanel({ state }: { state: ProductListingViewState }) {
  if (state.status === "idle") return null;
  const policyMeta = { eligible: ["Eligible candidate", "bg-[#effaf1] text-[#14752b]"], approval_needed: ["Approval needed", "bg-[#fff5ed] text-[#9a3b00]"], blocked: ["Blocked", "bg-[#fff1fa] text-[#9b2164]"], unverified: ["Unverified", "bg-[#f6f5f4] text-[#615d59]"] } as const;
  return <section className="mb-5 rounded-xl border border-[#e6e6e6] bg-white p-5 shadow-[0_1px_1px_rgba(0,0,0,.02),0_6px_18px_rgba(0,0,0,.025)]"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[12px] font-semibold uppercase tracking-[0.06em] text-[#0075de]">Confirmed product listings</p><h2 className="mt-1 text-[22px] font-bold tracking-[-0.45px] text-black">Products matching your confirmed request.</h2><p className="mt-1 max-w-[760px] text-[12px] leading-5 text-[#615d59]">{state.message ?? "Marketplace products are normalized in-app and classified deterministically from the confirmed price and authority boundaries."}</p></div><span className="rounded-full bg-[#edf6ff] px-2.5 py-1 text-[11px] font-bold text-[#005bab]">{state.status === "loading" ? "Searching" : `${state.listings.length} listings`}</span></div>{state.status === "loading" ? <div className="mt-5 h-32 animate-pulse rounded-lg bg-[#f6f5f4]" /> : <div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{state.listings.map(listing => { const [label, tone] = policyMeta[listing.policy]; return <article key={listing.id} className="overflow-hidden rounded-xl border border-[#e6e6e6] bg-[#fdfdfc]"><div className="flex gap-3 p-4">{listing.imageUrl ? <img src={listing.imageUrl} alt="" className="h-20 w-20 rounded-lg border border-[#e6e6e6] bg-white object-contain" /> : <div className="grid h-20 w-20 place-items-center rounded-lg bg-[#f0efed] text-[10px] font-bold text-[#615d59]">NO IMAGE</div>}<div className="min-w-0 flex-1"><p className="line-clamp-2 text-[14px] font-bold leading-5 text-[#31302e]">{listing.title}</p><p className="mt-1 text-[12px] text-[#615d59]">{listing.merchant ?? "Merchant unavailable"}</p><p className="mt-2 text-[18px] font-bold tabular-nums text-black">{listing.priceText ?? "Price unavailable"}</p></div></div><dl className="grid grid-cols-2 gap-x-3 gap-y-2 border-t border-[#e6e6e6] px-4 py-3 text-[11px]"><div><dt className="text-[#a39e98]">Availability</dt><dd className="mt-0.5 font-semibold text-[#31302e]">{listing.availability ?? "Unknown"}</dd></div><div><dt className="text-[#a39e98]">Delivery</dt><dd className="mt-0.5 font-semibold text-[#31302e]">{listing.delivery ?? "Unknown"}</dd></div><div><dt className="text-[#a39e98]">Rating</dt><dd className="mt-0.5 font-semibold text-[#31302e]">{listing.rating ? `${listing.rating}/5${listing.reviews ? ` · ${listing.reviews}` : ""}` : "Unknown"}</dd></div><div><dt className="text-[#a39e98]">Evidence</dt><dd className="mt-0.5 font-semibold text-[#31302e]">{listing.completeness === "complete" ? "Core fields present" : "Core fields missing"}</dd></div></dl><div className="flex items-center justify-between gap-2 border-t border-[#e6e6e6] px-4 py-3"><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${tone}`}>{label}</span>{listing.productUrl ? <a href={listing.productUrl} target="_blank" rel="noreferrer" className="text-[12px] font-bold text-[#005bab] hover:underline">View source →</a> : <span className="text-[11px] text-[#a39e98]">Source unavailable</span>}</div></article>; })}</div>}<p className="mt-4 text-[11px] font-semibold uppercase tracking-[0.06em] text-[#9a3b00]">Marketplace listings cannot create an order. Quantity, stock, delivery, returns, and seller terms require independent confirmation.</p></section>;
}
